{
  "name": "relead-ops",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ReLead Ops: Supabase-powered SSH and Cloudflare Tunnel operations control plane.",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "dev:web": "tsx watch src/index.ts",
    "dev:worker": "tsx watch src/worker-main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "start:web": "node dist/index.js",
    "start:worker": "node dist/worker-main.js",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/websocket": "^11.2.0",
    "@xterm/xterm": "^5.5.0",
    "fastify": "^5.2.0",
    "jose": "^5.10.0",
    "yaml": "^2.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
